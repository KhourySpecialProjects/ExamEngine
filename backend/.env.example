# Database
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/exam_engine_db

# LocalStack endpoint (only for local dev - MUST be removed/commented for production)
AWS_ENDPOINT_URL=http://localstack:4566

# AWS Credentials
# For LocalStack: use "test" for both (fake credentials)
# For Production: use real IAM credentials
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_REGION=us-east-1
AWS_S3_BUCKET=exam-engine-csvs

# Security
SECRET_KEY=generate_a_secure_random_string_here

# Application
ENVIRONMENT=development
DEBUG=True
FRONTEND_URL=http://localhost:3000

# Admin
ADMIN_PASSWORD=password
ADMIN_EMAIL=email
